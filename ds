const lv_image_dsc_t ic_notconnected = {
.header.cf = LV_COLOR_FORMAT_RGB565,
.header.magic = LV_IMAGE_HEADER_MAGIC,
.header.w = 96,
.header.h = 96,
.data_size = 9216 * 2,
.data = ic_notconnected_map,
}; 아이콘이 이렇게 정의 되어 있을때, // 🔹 아이콘 추가 (왼쪽 정렬)
extern const lv_img_dsc_t ic_notconnected;
ui->mpNetWorkInfoPage->mpImgNotConnectedStatus = CreateImage(ui->mpNetWorkInfoPage->mpLayoutNetworkStatus, &ic_notconnected, LV_ALIGN_LEFT_MID, 0, 0);
이렇게 쓰면 아이콘이 너무 커. 나는 이미지가 깨지지 말라고 일부러 x4 해서 변환해서 가져온거거든
