register_cni_update_handler

platform_ipc_helper::blocking::data_manager::facility::watch
pub fn cni_updated<F>(filter: FacilityFilter, cb: F) -> Result<()>
where
    F: Fn(&str) + Send + Sync + 'static,

init_cni_values
platform_ipc_helper::blocking::data_manager::facility
pub fn get_list_by_type(facility_type: &FacilityType) -> Result<Vec<FacilityInfo>>
